apiVersion: v1
kind: Service
metadata:
  name: {{ include "homecal.fullname" (dict "context" . "component" "web") }}
  labels:
    {{- include "homecal.labels" (dict "context" . "component" "web") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.web.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homecal.selectorLabels" (dict "context" . "component" "web") | nindent 4 }}
