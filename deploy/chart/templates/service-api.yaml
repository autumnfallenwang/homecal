apiVersion: v1
kind: Service
metadata:
  name: {{ include "homecal.fullname" (dict "context" . "component" "api") }}
  labels:
    {{- include "homecal.labels" (dict "context" . "component" "api") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "homecal.selectorLabels" (dict "context" . "component" "api") | nindent 4 }}
